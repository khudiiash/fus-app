/** Firestore / RTDB shared world used by the Laby js-minecraft client (voxel β successor). */
export const FUS_SHARED_WORLD_LABY_ID = 'fus-world-laby' as const

/** Canonical feet/world spawn for the shared Laby world (Firestore `labySpawn` + in-game default). */
export const FUS_LABY_CANONICAL_SPAWN = { x: 85, y: 74, z: -47 } as const
